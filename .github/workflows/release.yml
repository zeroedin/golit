name: Release

on:
  pull_request:
    types: [closed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.head_ref == 'release/next' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Get version
        id: version
        run: |
          VERSION=$(grep -oP 'Version\s*=\s*"\K[0-9]+\.[0-9]+\.[0-9]+' cmd/golit/version.go)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Extract release notes
        run: |
          awk '/^## /{if(n++)exit}n' CHANGELOG.md > /tmp/release-notes.md

      - name: Cross-compile
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          mkdir -p dist

          LDFLAGS="-s -w -X main.Version=${VERSION}"
          TARGETS=(
            "linux/amd64"
            "linux/arm64"
            "darwin/amd64"
            "darwin/arm64"
            "windows/amd64"
            "windows/arm64"
          )

          for target in "${TARGETS[@]}"; do
            GOOS="${target%/*}"
            GOARCH="${target#*/}"
            OUTPUT="dist/golit-${GOOS}-${GOARCH}"
            [ "$GOOS" = "windows" ] && OUTPUT="${OUTPUT}.exe"

            echo "Building ${GOOS}/${GOARCH}..."
            CGO_ENABLED=0 GOOS="$GOOS" GOARCH="$GOARCH" \
              go build -ldflags="$LDFLAGS" -o "$OUTPUT" ./cmd/golit/
          done

      - name: Collect third-party licenses
        run: |
          go install github.com/google/go-licenses@latest
          go-licenses save ./... --save_path=dist/THIRD_PARTY_LICENSES
          cp LICENSE dist/LICENSE

      - name: Package archives
        run: |
          set -euo pipefail
          cd dist

          for bin in golit-*; do
            [ ! -f "$bin" ] && continue
            name="${bin%.exe}"

            case "$bin" in
              *.exe)
                zip -r "${name}.zip" "$bin" LICENSE THIRD_PARTY_LICENSES
                rm "$bin"
                ;;
              *)
                tar czf "${name}.tar.gz" "$bin" LICENSE THIRD_PARTY_LICENSES
                rm "$bin"
                ;;
            esac
          done

          rm -rf LICENSE THIRD_PARTY_LICENSES
          sha256sum * > checksums.txt

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release create "v${VERSION}" dist/* \
            --title "v${VERSION}" \
            --notes-file /tmp/release-notes.md

      - name: Update Homebrew tap
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          gh workflow run update-formula.yml \
            --repo zeroedin/homebrew-golit \
            --field version="${{ steps.version.outputs.version }}"
